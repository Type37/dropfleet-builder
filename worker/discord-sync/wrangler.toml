name = "dfc-discord-sync"
main = "src/index.js"
compatibility_date = "2026-09-01"
workers_dev = true

# DISCORD_CLIENT_ID, DISCORD_CLIENT_SECRET and SYNC_SECRET are Worker secrets,
# set with `wrangler secret put`. See docs/AUTH-SETUP.md.
